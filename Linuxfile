# Linux package manifest — dnf (Fedora) equivalents of the Brewfile CLI layer.
# One package per line; lines starting with # are comments.
# Consumed by scripts/packages.sh.

# Run your GitHub Actions locally
act
# Record and share terminal sessions
asciinema
# Code searching, linting, rewriting (not in Fedora repos; alternatives:
#   npm -g @ast-grep/cli, or cargo install ast-grep)
# ast-grep
# Searches a binary image for embedded files and executable code
binwalk
# Cloudflare Tunnel client (not in Fedora repos; use Cloudflare's rpm repo:
#   https://pkg.cloudflare.com/)
# cloudflared
# Cross-platform make
cmake
# Play, record, convert, and stream select audio and video codecs
ffmpeg
# Quickly rewrite git repository history
git-filter-repo
# GNU Privacy Guard (OpenPGP)
gnupg
# Graph visualization software from AT&T and Bell Labs
graphviz
# retired from Fedora; `gh` covers GitHub PR workflows
# hub
# Tools and libraries to manipulate images in select formats
ImageMagick
# Tool to unpack installers created by Inno Setup
innoextract
# Send files easily, securely, and reliably (not in Fedora repos; use pipx)
# magic-wormhole
# Generate local development certificates
mkcert
# Hyperflexible text editor (Brewfile: neovim)
neovim
# Universal document converter
pandoc
# Line-oriented search tool (Rust)
ripgrep
# Shell script formatter and parser
shfmt
# Simple and flexible tool for managing secrets (not in Fedora repos; upstream
#   rpm from github.com/getsops/sops/releases, or copr)
# sops
# Fast real-time text search (Brewfile: the_silver_searcher)
the_silver_searcher
# Share terminals over the web
ttyd
# Z shell, a UNIX command interpreter
zsh
# Open source programming language to build simple/reliable/efficient software
golang
# Official Amazon AWS command-line interface
awscli